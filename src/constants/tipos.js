import { COLORS } from './colors';

export const TIPOS_FILTRO = ['Todos', 'UBS', 'Posto', 'CAPS'];

export const TIPO_CORES = {
  UBS: COLORS.tipoUBS,
  Posto: COLORS.tipoPosto,
  CAPS: COLORS.tipoCAPS,
  'Urgência': COLORS.tipoUrgencia,
};
